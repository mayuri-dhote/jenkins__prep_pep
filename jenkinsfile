pipeline {
    agent none

    stages{

        stage (build) {
            steps {
                sh 'mvn compile'
            }
        
             }
        stage (test) {
            steps {
                sh 'mvn clean test'
            }
        }
        stage (package) {
            steps {
                sh 'mvn clean package'
            }
        }


    }
}