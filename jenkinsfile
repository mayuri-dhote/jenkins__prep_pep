pipeline {
    agent none

    stages{

        stage {
            steps {
                sh 'mvn build'
            }
        
             }
        stage {
            steps {
                sh 'mvn clean test'
            }
        }
        stage {
            steps {
                sh 'mvn clean package'
            }
        }


    }
}