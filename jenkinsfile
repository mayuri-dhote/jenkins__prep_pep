pipeline {
    agent none

    stages{

        stage (build) {
            steps {
                sh 'mvn build'
            }
        
             }
        stage (test) {
            steps {
                sh 'mvn clean test'
            }
        }
        stage (package) {
            steps {
                sh 'mvn clean package'
            }
        }


    }
}