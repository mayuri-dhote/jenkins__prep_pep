pipeline {
    agent none

    stages{

        stage (build) {
            step {
                sh 'mvn build'
            }
        
             }
        stage (test) {
            step {
                sh 'mvn clean test'
            }
        }
        stage (package) {
            step {
                sh 'mvn clean package'
            }
        }


    }
}